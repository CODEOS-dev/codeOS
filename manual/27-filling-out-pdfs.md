# 填写 PDF

codeOS 预装了一个不错的基础 PDF 查看器叫 Document Viewer。双击 PDF 文件就用它打开。

但 Document Viewer 只能填写已设置为表单的 PDF。如果需要填写没有表单的 PDF，或者需要给 PDF 签名，就得右键文件选 _Open With..._，挑 Xournal++。

Xournal++ 里用 T 工具可以在 PDF 任意位置书写。要签名的话，需要先有签名图片，然后用 Image 工具插入并调整大小。

填完 PDF 后，用 _File > Export as PDF_ 保存最终版本。
