#!/bin/bash

# omarchy:summary=Print the active codeOS mirror and package channel

if grep -q "https://stable-mirror.codeos.org/" /etc/pacman.d/mirrorlist; then
  mirror="stable"
elif grep -q "https://rc-mirror.codeos.org/" /etc/pacman.d/mirrorlist; then
  mirror="rc"
elif grep -q "https://mirror.codeos.org/" /etc/pacman.d/mirrorlist; then
  mirror="edge"
else
  mirror="unknown"
fi

if grep -q "https://pkgs.codeos.org/stable/" /etc/pacman.conf; then
  pkgs="stable"
elif grep -q "https://pkgs.codeos.org/rc/" /etc/pacman.conf; then
  pkgs="rc"
elif grep -q "https://pkgs.codeos.org/edge/" /etc/pacman.conf; then
  pkgs="edge"
else
  pkgs="unknown"
fi

if [[ $mirror == $pkgs ]]; then
  echo $mirror
else
  echo "$mirror / $pkgs"
fi
